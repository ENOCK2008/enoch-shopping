version: 1
services:
  - type: web
    name: enoch-shopping
    env: python
    buildCommand: pip install -r requirements.txt && python manage.py migrate
    startCommand: "gunicorn enoch_shopping.wsgi:application --bind 0.0.0.0:$PORT"
    plan: free
